'use strict';

import React, {
  StyleSheet,
} from 'react-native'

var styles = StyleSheet.create({
  mainView: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
  titleText: {
    fontSize: 40,
  },
  circle: {
    backgroundColor: '#bbbbbb',
    height: 80,
    width: 80,
    borderRadius: 40,
    justifyContent: 'center',
    alignItems: 'center',
  }
});

module.exports = styles;
