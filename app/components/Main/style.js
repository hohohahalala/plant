'use strict';

import React, {
  StyleSheet,
} from 'react-native'

var styles = StyleSheet.create({
  mainView: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
  titleText: {
    fontSize: 40,
  },
  circle: {
    backgroundColor: '#bbbbbb',
    height: 50,
    width: 50,
    borderRadius: 25
  }
});

module.exports = styles;
